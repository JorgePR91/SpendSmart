{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
"homepage": "https://jorgepr91.github.io/SpendSmart",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
},
"scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
	}


}
